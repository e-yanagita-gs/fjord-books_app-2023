<%= form_with model: [commentable, comment] do |f| %>
  <div>
    <%= f.label t('views.common.new', name: Comment.model_name.human) %>
    <%= f.text_area :content, required: true %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
